import { FigmaFile, FigmaPartialFile, FigmaSharedNode, FigmaTeamComponent, FigmaTeamStyle, FigmaProjectDetails, FigmaVersion } from "./models/figma";
/**
 * Static class used to call the REST API
 */
export declare class FigmaAPIHelper {
    static API_TOKEN: string;
    static setToken(token: string): void;
    static getTeamProjects(teamIds: string[]): Promise<FigmaProjectDetails[]>;
    static getProjectFiles(projectId: string): Promise<FigmaPartialFile[]>;
    static getFile(fileKey: string): Promise<FigmaFile>;
    static getFileHistory(fileKey: string): Promise<FigmaVersion[]>;
    static getTeamComponentSets(teamId: string): Promise<FigmaTeamComponent[]>;
    static getTeamComponents(teamId: string): Promise<FigmaTeamComponent[]>;
    static getTeamStyles(teamId: string): Promise<FigmaTeamStyle[]>;
    static getFileComponents(fileKeys: string[]): Promise<FigmaTeamComponent[]>;
    static getFileStyles(fileKeys: string[]): Promise<FigmaTeamStyle[]>;
    static getNodeDetails(fileKey: string, nodeIds: string[]): Promise<{
        nodes: {
            [key: string]: any;
        };
    }>;
    /**
     * Breaks a list of styles into a relevant file key with styles so we can lookup by file
     */
    static createFileBuckets(sharedNodes: FigmaSharedNode[]): {
        nodeIdMap: {
            [id: string]: FigmaSharedNode;
        };
        fileBuckets: {
            [key: string]: string[];
        };
    };
}
