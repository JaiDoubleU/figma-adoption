/// <reference types="figma" />
import { FigmaStyleType, StyleLookupMap } from "../../models/figma";
import { LintCheckName, LintCheck } from "../../models/stats";
/**
 * Check if any sub properties overlap and amtch
 * @param checkName
 * @param styles
 * @param propertiesToCheck - list of names to lookup
 * @param targetNode
 * @returns
 */
export default function getStyleLookupMatches(checkName: LintCheckName, stylesLookup: StyleLookupMap, styleType: FigmaStyleType | "STROKE", targetNode: BaseNode): LintCheck;
