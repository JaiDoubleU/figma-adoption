/// <reference types="figma" />
import { StyleBucket, FigmaStyleType, PropertyCheck } from "../../models/figma";
import { LintCheckName, LintCheck } from "../../models/stats";
/**
 * Check if any sub properties overlap and amtch
 * @param checkName
 * @param styles
 * @param propertiesToCheck
 * @param targetNode
 * @returns
 */
export default function getPartialStyleMatches(checkName: LintCheckName, stylesBucket: StyleBucket, styleType: FigmaStyleType, propertiesToCheck: PropertyCheck[], targetNode: BaseNode, opts?: {
    union: boolean;
}): LintCheck;
