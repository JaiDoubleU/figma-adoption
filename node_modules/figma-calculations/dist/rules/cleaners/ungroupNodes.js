"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ungroupNodes = void 0;
/**
 * Ungroups a set of nodes that shouldn't be grouped
 * @param nodes
 */
function ungroupNodes(nodes) {
    for (let i = 0; i < nodes.length; i++) {
        const groups = nodes[i].findAll((n) => n.type === 'GROUP' && n.children && n.children.length === 1);
        for (let j = 0; j < groups.length; j++) {
            const group = groups[j];
            return;
        }
    }
}
exports.ungroupNodes = ungroupNodes;
