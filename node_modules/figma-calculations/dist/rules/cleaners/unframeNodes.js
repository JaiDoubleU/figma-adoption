"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.unframeNodes = void 0;
function unframeNodes(nodes) {
    for (let i = 0; i < nodes.length; i++) {
        const frames = nodes[i].findAll((n) => n.type === 'FRAME' && n.layoutMode === 'NONE' && n.clipsContent === false && n.parent.type !== 'FRAME');
        for (let j = 0; j < frames.length; j++) {
            let frame = frames[j];
            if (!Array.isArray(frame.fills) && !frame.fillStyleId) {
                try {
                    figma.ungroup(frame);
                }
                catch (e) { }
            }
        }
    }
}
exports.unframeNodes = unframeNodes;
