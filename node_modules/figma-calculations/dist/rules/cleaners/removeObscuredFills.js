"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeObscuredFills = void 0;
//figma.currentPage.findAll(n => (n.fills && n.fills != figma.mixed && n.fills.length > 1))
function removeObscuredFills() {
    const multiFills = figma.currentPage.findAll((n) => Array.isArray(n.fills) && n.fills.length > 1);
    for (let i = 0; i < multiFills.length; i++) {
        const n = multiFills[i];
        const fills = [];
        if (n.fills && Array.isArray(n.fills)) {
            for (let j = n.fills.length - 1; j >= 0; j--) {
                console.log(n.fills[j]);
                if (n.fills[j].opacity < 1) {
                    fills.push(n.fills[j]);
                }
                else if (n.fills[j].opacity === 1) {
                    fills.push(n.fills[j]);
                    break;
                }
            }
            fills.reverse();
            n.fills = fills;
        }
    }
}
exports.removeObscuredFills = removeObscuredFills;
