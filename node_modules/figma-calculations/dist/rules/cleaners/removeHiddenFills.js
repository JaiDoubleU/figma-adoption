"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeHiddenFills = void 0;
const __1 = require("../..");
function removeHiddenFills(startNode) {
    // @ts-ignore: Unreachable code error
    const hiddenFills = __1.FigmaCalculator.findAll(startNode, (n) => n.fills);
    for (var i = 0; i < hiddenFills.length; i++) {
        var n = hiddenFills[i];
        var fills = [];
        if (n.fills && Array.isArray(n.fills)) {
            for (var j = 0; j < n.fills.length; j++) {
                if (n.fills[j].visible && n.fills[j].opacity > 0) {
                    fills.push(n.fills[j]);
                }
            }
            n.fills = fills;
        }
    }
}
exports.removeHiddenFills = removeHiddenFills;
