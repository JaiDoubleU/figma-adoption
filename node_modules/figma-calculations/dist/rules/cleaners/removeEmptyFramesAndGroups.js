"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeEmptyFramesAndGroups = void 0;
const __1 = require("../..");
function removeEmptyFramesAndGroups(startNode) {
    const frames = __1.FigmaCalculator.FindAll(startNode, (n) => n.type === "FRAME" || n.type === "GROUP");
    for (let j = 0; j < frames.length; j++) {
        const frame = frames[j];
        const hasNoChildren = !frame.children || frame.children.length === 0;
        const hasNoFills = !Array.isArray(frame.fills) || !frame.fills || frame.fills.length === 0;
        const hasNoFillStyle = !frame.fillStyleId ||
            (frame.styles && !frame.styles.fill);
        if (hasNoChildren && hasNoFills && hasNoFillStyle) {
            try {
                __1.FigmaCalculator.RemoveNode(startNode, frame.id);
            }
            catch (e) { }
        }
    }
}
exports.removeEmptyFramesAndGroups = removeEmptyFramesAndGroups;
