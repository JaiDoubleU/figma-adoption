/// <reference types="figma" />
export interface Counter {
    total: number;
    hidden: number;
    topLevel?: number;
    types?: Record<string, number>;
}
/** Checks if a node can have child nodes
 * @param {SceneNode} node a node in question
 */
export declare function supportsChildren(node: SceneNode): node is FrameNode | GroupNode | ComponentNode | InstanceNode | BooleanOperationNode;
/** Counts layers taking their types into account
 * @param {SceneNode[]} nodes array of nodes to count
 */
export declare function countLayers(nodes: SceneNode[]): Counter;
