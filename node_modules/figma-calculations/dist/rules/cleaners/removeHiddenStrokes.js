"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeHiddenStrokes = void 0;
//figma.currentPage.findAll(n => (n.fills && n.fills != figma.mixed && n.fills.length > 1))
function removeHiddenStrokes() {
    // @ts-ignore: Unreachable code error
    const hiddenStrokes = figma.currentPage.findAll((n) => n.strokes);
    for (var i = 0; i < hiddenStrokes.length; i++) {
        var n = hiddenStrokes[i];
        var strokes = [];
        if (n.strokes) {
            for (var j = 0; j < n.strokes.length; j++) {
                //console.log(n.fills[j])
                if (n.strokes[j].visible && n.strokes[j].opacity > 0) {
                    strokes.push(n.strokes[j]);
                }
            }
            n.strokes = strokes;
        }
    }
}
exports.removeHiddenStrokes = removeHiddenStrokes;
