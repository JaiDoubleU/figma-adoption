"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.unlockNodes = void 0;
function unlockNodes() {
    const lockedNodes = figma.currentPage.findAll((n) => n.locked === true);
    for (let i = 0; i < lockedNodes.length; i++) {
        try {
            lockedNodes[i].locked = false;
        }
        catch (e) { }
    }
}
exports.unlockNodes = unlockNodes;
