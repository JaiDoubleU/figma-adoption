"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeTransparentNodes = void 0;
const __1 = require("../..");
function removeTransparentNodes(startNode) {
    const hiddenNodes = __1.FigmaCalculator.FindAll(startNode, (n) => n.opacity && n.opacity === 0);
    for (let i = 0; i < hiddenNodes.length; i++) {
        try {
            __1.FigmaCalculator.RemoveNode(startNode, hiddenNodes[i].id);
        }
        catch (e) {
            //console.log('Could not remove ' + hiddenNodes[i]);
        }
    }
}
exports.removeTransparentNodes = removeTransparentNodes;
