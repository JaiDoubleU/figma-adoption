import { FigmaStyleType, StyleBucket } from "../models/figma";
export declare const isExactStyleMatch: (styleType: FigmaStyleType | "STROKE", styleBucket: StyleBucket, targetNode: SceneNode) => boolean;
