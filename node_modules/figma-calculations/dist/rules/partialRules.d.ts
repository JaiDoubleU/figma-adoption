/// <reference types="figma" />
import { FigmaTeamStyle, StyleBucket } from "../models/figma";
import { LintCheck, MatchLevel } from "../models/stats";
export declare function handleCountsIncrement(name: string, matchLevel: MatchLevel, counts: any): void;
export declare function generateStyleBucket(styles: FigmaTeamStyle[]): StyleBucket;
/**
 * Check if a Node Type overlaps
 * @param types
 * @param node
 * @returns
 */
export declare function isNodeOfTypeAndVisible(types: string[], node: SceneNode): boolean;
/**
 * Run through all partial matches, and make exceptions depending on rules
 */
export declare const runSimilarityChecks: (styleBucket: StyleBucket, targetNode: SceneNode) => LintCheck[];
